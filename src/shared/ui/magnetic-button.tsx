import React from 'react'; import { motion } from 'framer-motion'; import { ArrowRight } from 'lucide-react'; export function MagneticButton({ children, primary = true }: { children: React.ReactNode, primary?: boolean }) { return ( <motion.button whileHover={{ scale: 1.02, y: -1 }} whileTap={{ scale: 0.99 }} transition={{ type: 'spring', stiffness: 300, damping: 20 }} className={`relative overflow-hidden rounded-xl px-6 h-12 inline-flex items-center justify-center gap-2 font-bold tracking-wide transition-all duration-300 group font-sans text-xs uppercase cursor-pointer ${primary ? 'bg-[#2962FF] hover:bg-[#1E4FD8] text-white shadow-lg shadow-[#2962FF]/20' : 'border border-white/10 bg-white/[0.03] hover:bg-white/[0.06] text-white backdrop-blur-md'}`}> <span className="relative z-10 flex items-center gap-2"> {children} <ArrowRight className="w-3.5 h-3.5 transition-transform duration-200 group-hover:translate-x-0.5" /> </span> </motion.button> ); }
